{
  "name": "tokenvote",
  "version": "1.0.0",
  "description": "A decentralized voting system for token holders on Stacks",
  "main": "index.js",
  "scripts": {
    "test": "clarinet test",
    "deploy:testnet": "npx ts-node scripts/deploy.ts",
    "deploy:mainnet": "set NETWORK=mainnet && npx ts-node scripts/deploy.ts",
    "check": "clarinet check",
    "console": "clarinet console",
    "dev:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "install:frontend": "cd frontend && npm install"
  },
  "keywords": [
    "stacks",
    "clarity",
    "voting",
    "governance",
    "blockchain",
    "dapp"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@stacks/cli": "^6.0.0",
    "@stacks/network": "^6.0.0",
    "@stacks/transactions": "^6.0.0",
    "@types/node": "^18.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^4.9.0"
  },
  "dependencies": {
    "@stacks/connect": "^7.0.0"
  }
}
